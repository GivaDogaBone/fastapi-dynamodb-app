   option_settings:
     aws:autoscaling:launchconfiguration:
       LaunchConfiguration: null # Disables Launch Configuration to avoid conflicts
     aws:autoscaling:launchtemplate:
       LaunchTemplate: true # Ensures Launch Templates are created for Auto Scaling